import { MenuLateralDirective } from './menu-lateral.directive';

describe('MenuLateralDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuLateralDirective();
    expect(directive).toBeTruthy();
  });
});
